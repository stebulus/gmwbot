#!/usr/bin/env python
import gmwbot
import gmwbottest

words = []
with open('twl') as twl:
    for line in twl:
        words.append(line.rstrip())
search = gmwbot.binarysearcher(words)
for word in ['taco', 'orrery', 'alibaba']:
    print 'Searching for', repr(word)
    result = search(gmwbottest.cmplog(word))
    print 'result:', result
    print
